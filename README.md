# VAI
A script that shortens a video based on its volume.
#### Note: Still in the test phase

## Requirements
```bash
pip install numpy
pip install moviepy
pip install pyfiglet
```

## Usage
```bash
VAI.py [Filename]
```
